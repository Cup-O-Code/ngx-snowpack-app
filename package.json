{
  "name": "ngx-snowpack-app",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "dev": "ngsnow",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "dependencies": {    
  },
  "devDependencies": {
    "@snowpack/plugin-sass": "^1.4.0",
    "angular-snowpack-plugin": "^2.3.1",
    "snowpack": "^3.8.3"
  }
}
